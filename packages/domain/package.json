{
  "name": "@afrihost/domain",
  "version": "0.1.0",
  "private": true,
  "description": "AfriHost AI — modules métier (domaine) : services, règles, validation. Dépend de @afrihost/core (infrastructure).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@afrihost/core": "*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
