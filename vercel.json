{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm ci && npm run build -w @afrihost/core && npm run build -w @afrihost/domain && npm --prefix apps/web run build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "regions": ["fra1"]
}
